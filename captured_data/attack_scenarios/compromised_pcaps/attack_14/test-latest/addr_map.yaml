# ---------------- Registers (unique numeric keys) ----------------
1024: start_cmd              # %MW0 (holding register)
100:  tank_level             # %IW100 (input register)
102:  process_state          # %QW100 (output register/state word)
804:  mixing_done            # %QX100.4 (coil/discrete)

# Choose unique addresses for these bits so they don't collide.
# If your Zeek logs truly show 0,1,2 for the coils, keep them;
# move alarm_flag to its real input address (e.g., 200) so keys are unique.
0:    inlet_valve            # %QX100.0 (coil)
1:    outlet_valve           # %QX100.1 (coil)
2:    mixer_ctrl             # %QX100.2 (coil)
200:  alarm_flag             # %IX100.0 (discrete input)  <-- was '0' before; make it unique here

# ---------------- Assets ----------------
assets:
  10.10.10.10: "PLC1 – OpenPLC Runtime"
  10.10.10.1:  "pfSense – OT Gateway"
  10.10.20.30: "HMI – AdvancedHMI (Windows)"
  10.10.20.1:  "pfSense – IT Gateway"
